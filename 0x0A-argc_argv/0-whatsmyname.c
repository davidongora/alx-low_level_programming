#include <stdio.h>
/**
 * main - prints out its program
 * @argc: program commands
 * @argv: program array commands
 * Return: o if successful
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i =0;

	printf("%s\n", argv[i]);
	return (0);
}
